CREATE TABLE persons (
                                id int PRIMARY KEY,
                                firstName varchar(255),
                                lastName varchar(255),
                                address varchar(255)
);

CREATE TABLE pets (
                         id int PRIMARY KEY,
                         petName varchar(255),
                         breed varchar(255)
);